<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gerador de Senhas Seguras | Password Pro</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <div class="container">
        <h1>Gerador de Senhas Seguras</h1>
        
        <div class="password-display">
            <input type="password" class="password-field" id="password" readonly>
            <button class="toggle-password" id="togglePassword" title="Mostrar/Esconder senha">
                <i class="fas fa-eye"></i>
            </button>
            <button class="copy-password" id="copyPassword" title="Copiar senha">
                <i class="fas fa-copy"></i>
            </button>
            <div class="strength-meter">
                <div class="strength-bar" id="strengthBar"></div>
            </div>
            <div class="strength-text" id="strengthText">Força da senha: muito fraca</div>
        </div>
        
        <div class="controls">
            <div class="control-group">
                <label for="length">Tamanho da senha: <span class="length-value" id="lengthValue">12</span></label>
                <div class="slider-container">
                    <input type="range" id="length" class="slider" min="4" max="32" value="12">
                </div>
            </div>
            
            <div class="control-group">
                <label>Incluir:</label>
                <div class="checkbox-group">
                    <div class="checkbox-container">
                        <input type="checkbox" id="uppercase" checked>
                        <label for="uppercase">Letras maiúsculas (A-Z)</label>
                    </div>
                    <div class="checkbox-container">
                        <input type="checkbox" id="lowercase" checked>
                        <label for="lowercase">Letras minúsculas (a-z)</label>
                    </div>
                    <div class="checkbox-container">
                        <input type="checkbox" id="numbers" checked>
                        <label for="numbers">Números (0-9)</label>
                    </div>
                    <div class="checkbox-container">
                        <input type="checkbox" id="symbols" checked>
                        <label for="symbols">Símbolos (!@#$%^&*)</label>
                    </div>
                </div>
            </div>
            
            <div class="control-group">
                <div class="checkbox-container">
                    <input type="checkbox" id="excludeSimilar">
                    <label for="excludeSimilar">Excluir caracteres similares (e.g., i, l, 1, L, o, 0, O)</label>
                </div>
            </div>
        </div>
        
        <button class="btn" id="generate">Gerar Senha</button>
    </div>
    
    <div class="notification" id="notification">
        Senha copiada para a área de transferência!
    </div>
    
    <script src="main.js"></script>
</body>
</html>